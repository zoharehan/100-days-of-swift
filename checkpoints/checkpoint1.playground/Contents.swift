import UIKit

//CHECKPOINT 1: Celcius to Fahrenheit

//since C and F are usually decimal numbers we use doubles
// by adding decimal points to our values.

let celcius = 45.3
let fahrenheit = ((celcius*9)/5) + 32

print("\(celcius)°C is \(fahrenheit)°F.")
